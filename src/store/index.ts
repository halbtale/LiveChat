import { createStore } from 'vuex'

export default createStore({
  state: {
    userState: {
      username: '',
      chatName: '',
      chatNameArray: new Set(),
    }
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
